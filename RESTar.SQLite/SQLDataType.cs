namespace RESTar.SQLite {
    public enum SQLDataType
    {
        Unsupported = 0,
        SMALLINT,
        INT,
        BIGINT,
        SINGLE,
        DOUBLE,
        DECIMAL,
        TINYINT,
        TEXT,
        BOOLEAN,
        DATETIME
    }
}